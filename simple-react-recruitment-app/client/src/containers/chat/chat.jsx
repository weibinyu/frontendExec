import React,{useState} from 'react'
import {connect} from 'react-redux'

function Chat(props){
  return (
      <>
        Chat
      </>
  )
}

export default connect(
  state =>({}),
    {}
)(Chat)